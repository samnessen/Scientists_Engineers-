/****************************************************
**** Conversions of common units for engineering ***
****************************************************/
#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    char tempType;
    double tempUnit, farhenheit, celsius;

    cout << " Program is a conversion of temperature units for fahrenheit, celsius, & kelvin (note: precision is at 2)\n";
    cout << "enter f, c, or k to define current temperature units \n";
    cin >> tempUnit;

    //out put command line formats
    cout << setiosflags (ios::fixed)
         << setiosflags (ios::showpoint)
         << setprecision(2);

    if (tempType == 'f')
    {
        celsius = (5.0 / 9.0) * tempUnit + 32.0;
        cout << "\n Fahrenheit -> Celsius is: " << celsius << endl;
    }
    else
	{
        farhenheit = (9.0 / 5.0) * tempUnit + 32.0;
        cout << "\n Celsius -> Fahrenheit is: " << farhenheit << endl;
	}

    return 0;
}
